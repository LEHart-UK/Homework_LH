---
title: "R Notebook"
output: html_notebook
---

```{r}
library(ggplot2)
library(CodeClanData)

qb_revenue_breakdown
qb_monthly_sales
qb_competitors
```
```{r}
#Graph1
ggplot(qb_revenue_breakdown) +
  geom_bar(aes(x = Year, y = Revenue, fill = Product), stat = "identity") 
 
```



Graph2
```{r}
str(qb_monthly_sales)
```
```{r}
class(Date)

```

```{r}
Month_Date <- data.frame(qb_monthly_sales)

head(Month_Date)
  
```
```{r}

Monthly_sales_Kw <- data.frame(
  Date = as.Date(names(tapply(Monthly_sales_Kw$Amount, Monthly_sales_Kw$Date, sum))),
  Amount = as.numeric(tapply(Monthly_sales_Kw$Amount, Monthly_sales_Kw$Date, sum))
)
                             
#ggplot(Monthly_sales_Kw, aes(x = date,y = Amount)) +
  geom_line(colour = red) +
  scale_x_date(format = "%b-%y") +
  xlab("") +
  ylab("Over time")
```

 
Graph3
```{r}
ggplot(qb_monthly_sales) +
  geom_line(aes(x = Date, y = "Amount"), group = "Amount", stat = "identity")
  
```

